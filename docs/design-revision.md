# Reference fidelity revision

- Prospects: recreate the original map-first composition from `Prospects-Client`: full-viewport Google Map, top-left New Prospect, top-right menu, lower-left map key, right-side search/menu, filters, discovery, reporting, and prospect details. Apply only the Sync portal palette and typography, not the portal's sidebar layout. Synthetic prospects use city-center coordinates, clearly labeled.
- Helga: match the original `helga/ui/index.html`: 200px collapsible sidebar, compact metadata strip, calendar and inbox around the amber network orb, four compact summary widgets, thinking/activity and transcript panels. Keep actual guest-workspace tasks, calendar, drafts, Claude chat, recording, transcription, and speech. Financial/account panels explain their demo scope instead of inventing live connections.
- Homepage: reference https://khanhnguyen.design/ as inspected in a desktop and mobile browser: warm charcoal/ivory, narrow vertical rail, enormous light editorial serif, generous asymmetric space, chapter labels, and large project images. Use Jake's own content and app screenshots. Use a responsive chapter layout with native anchors and reduced-motion support.
- Google Maps: retrieve `/sync/google/api-key` from SSM at runtime through the authenticated demo API. This existing key is documented in the KB as a browser-exposed Maps key with referrer restrictions. No key in source, build artifacts, files, or logs. Verify authorization on the deployed hostname; do not broaden shared key restrictions.
- Preserve the other four demos and guest workspace isolation. Deploy only the existing portfolio Lambda/policy and static tenant prefix. No new resources required.
